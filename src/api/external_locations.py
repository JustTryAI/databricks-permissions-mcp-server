"""
Databricks External Locations Permissions API

This module would provide functions for managing permissions for external locations in Databricks Unity Catalog.
Currently, the external locations permissions are managed through the general permissions API.
"""

import logging

logger = logging.getLogger(__name__)

# Note: External location permissions are managed through the general permissions API
# See src/api/permissions.py for the implementation of permissions management functions 